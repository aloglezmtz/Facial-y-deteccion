git init, git add ., git commit -m "primer commit", vincula el repositorio remoto con git remote add origin y sube con git push


git add .
git commit -m "Fix: corrección de dependencias y versiones en xxxx"

git push origin main
